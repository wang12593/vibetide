# home-sidebar-simplify 详细测试用例

## 测试概览

| 模块 | P0 | P1 | P2 | P3 | 总计 | 可自动化 |
|------|:--:|:--:|:--:|:--:|:----:|:-------:|
| 侧边栏导航 | 3 | 3 | 1 | 1 | 8 | 3 |
| 更多菜单 | 2 | 2 | 1 | 0 | 5 | 2 |
| 管理后台入口 | 2 | 1 | 0 | 1 | 4 | 2 |
| 首页配置隐藏 | 1 | 1 | 1 | 0 | 3 | 1 |
| 响应式布局 | 0 | 3 | 1 | 0 | 4 | 1 |
| 回归验证 | 1 | 1 | 0 | 0 | 2 | 0 |
| **合计** | **9** | **11** | **4** | **2** | **26** | **9 (35%)** |

## 测试环境

| 项目 | 说明 |
|------|------|
| 应用地址 | `http://localhost:3000` |
| 浏览器 | Chrome 最新版（主要）、Safari（辅助） |
| 测试账号 | 超管账号、普通用户账号（editor 角色） |

---

## 模块一：侧边栏导航

### TC-N01: 一级导航只显示首页和对话（P0）

| 项目 | 内容 |
|------|------|
| **ID** | TC-N01 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 访问 `/home` 2. 观察侧边栏一级导航区域 |
| **预期结果** | 侧边栏只显示 2 个一级导航项：「首页」（Home 图标）和「对话」（MessageSquare 图标）。不显示「任务」「员工」「技能」「场景」「知识」 |
| **代码位置** | `app-sidebar.tsx` L106-109 `NAV_ITEMS` |
| **可自动化** | ✅ Playwright: `expect(sidebar.locator('.nav-link')).toHaveCount(2)` |

### TC-N02: 一级导航点击跳转正确（P0）

| 项目 | 内容 |
|------|------|
| **ID** | TC-N02 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 点击「首页」2. 确认 URL 为 `/home` 3. 点击「对话」4. 确认 URL 为 `/chat` |
| **预期结果** | 每次点击后 URL 正确变更，页面内容对应切换 |
| **代码位置** | `app-sidebar.tsx` L468 `NavLink` |
| **可自动化** | ✅ Playwright: `await page.click('text=首页'); expect(page).toHaveURL('/home')` |

### TC-N03: 当前路由高亮正确（P0）

| 项目 | 内容 |
|------|------|
| **ID** | TC-N03 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 访问 `/home` 2. 观察「首页」导航项是否高亮 3. 访问 `/chat` 4. 观察「对话」导航项是否高亮 |
| **预期结果** | 当前页面对应的导航项有高亮样式（`nav-selected` class），其他项无高亮 |
| **代码位置** | `app-sidebar.tsx` L128-131 `isHrefActive()` |
| **可自动化** | ✅ Playwright: 检查 active class |

### TC-N04: 子路由也能正确高亮父导航（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-N04 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 访问 `/chat?conversation=xxx` 2. 观察「对话」导航项是否仍然高亮 |
| **预期结果** | `/chat` 的子路由 `/chat?conversation=xxx` 下，「对话」项仍高亮（`isHrefActive` 使用 `pathname.startsWith(href + "/")`） |
| **代码位置** | `app-sidebar.tsx` L130 |

### TC-N05: 空权限时一级导航仍可见（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-N05 |
| **前置条件** | 权限加载超时或返回空数组 |
| **步骤** | 1. 模拟 `permissions = []` 2. 观察侧边栏 |
| **预期结果** | 一级导航仍可见（`canSeeItem` 在 `permissions.length === 0` 时返回 `true`，容错策略） |
| **代码位置** | `app-sidebar.tsx` L418-419 |

### TC-N06: 菜单权限过滤生效（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-N06 |
| **前置条件** | 用户有部分权限（如没有 `menu:skills`） |
| **步骤** | 1. 设置用户缺少 `menu:skills` 权限 2. 打开「更多」菜单 3. 检查是否缺少「技能」项 |
| **预期结果** | 无权限的菜单项被过滤掉，有权限的项正常显示 |
| **代码位置** | `app-sidebar.tsx` L429 `MORE_ITEMS.filter` |

### TC-N07: 品牌区域显示穆兰头像（P2）

| 项目 | 内容 |
|------|------|
| **ID** | TC-N07 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 观察侧边栏顶部品牌区域 |
| **预期结果** | 展开模式：显示穆兰圆形头像（72x72）+ "穆兰"文字。收起模式：只显示头像（36x36） |
| **代码位置** | `app-sidebar.tsx` L437-453 |

### TC-N08: 侧边栏宽度过渡动画（P3）

| 项目 | 内容 |
|------|------|
| **ID** | TC-N08 |
| **前置条件** | 桌面端 |
| **步骤** | 1. 点击展开/收起按钮 2. 观察宽度变化 |
| **预期结果** | 侧边栏在 200px 和 60px 之间平滑过渡（`transition-[width] duration-300`） |
| **代码位置** | `app-sidebar.tsx` L433-435 |

---

## 模块二：更多菜单

### TC-M01: 更多菜单展开显示 6 个子项（P0）

| 项目 | 内容 |
|------|------|
| **ID** | TC-M01 |
| **前置条件** | 以任意角色登录，侧边栏展开模式 |
| **步骤** | 1. 点击「更多」按钮 2. 观察子菜单列表 |
| **预期结果** | 显示 6 个子项：任务、员工、技能、场景、知识、穆兰配置。每项有正确的图标和文字 |
| **代码位置** | `app-sidebar.tsx` L111-118 `MORE_ITEMS` |
| **可自动化** | ✅ Playwright |

### TC-M02: 更多菜单子项点击跳转正确（P0）

| 项目 | 内容 |
|------|------|
| **ID** | TC-M02 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 逐一点击更多菜单中的 6 个子项，验证跳转 |
| **预期结果** | 任务→`/missions`，员工→`/ai-employees`，技能→`/skills`，场景→`/workflows`，知识→`/knowledge-bases`，穆兰配置→`/settings/mulan-config` |
| **代码位置** | `app-sidebar.tsx` L139-158 `SubMenuList` |
| **可自动化** | ✅ Playwright |

### TC-M03: 更多菜单中当前路由高亮（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-M03 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 访问 `/skills` 2. 打开更多菜单 3. 观察「技能」项是否高亮 |
| **预期结果** | 「技能」项有 `nav-selected-sub` 高亮样式 |
| **代码位置** | `app-sidebar.tsx` L149 |

### TC-M04: 收起模式下更多菜单通过 Popover 显示（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-M04 |
| **前置条件** | 桌面端，侧边栏收起模式（60px） |
| **步骤** | 1. 鼠标悬停在设置/更多图标上 2. 观察 Popover 弹出 |
| **预期结果** | Popover 弹出显示所有 MORE_ITEMS 子项，可点击跳转 |
| **代码位置** | `app-sidebar.tsx` L163-179 `SettingsButton` |

### TC-M05: 无更多菜单项时不显示按钮（P2）

| 项目 | 内容 |
|------|------|
| **ID** | TC-M05 |
| **前置条件** | 用户没有任何 MORE_ITEMS 对应的权限（极端情况） |
| **步骤** | 1. 设置 `visibleMore.length === 0` 2. 观察侧边栏 |
| **预期结果** | 「更多」按钮不显示（L473 条件 `visibleMore.length > 0`） |
| **代码位置** | `app-sidebar.tsx` L473 |

---

## 模块三：管理后台入口

### TC-A01: 超管可见管理后台入口（P0）

| 项目 | 内容 |
|------|------|
| **ID** | TC-A01 |
| **前置条件** | 以超级管理员登录 |
| **步骤** | 1. 观察侧边栏底部 2. 确认有「管理后台」区域 |
| **预期结果** | 展开模式：底部显示"管理后台"分隔线 + 3 个子项（用户管理、角色权限、组织管理）。收起模式：显示齿轮图标 |
| **代码位置** | `app-sidebar.tsx` L476-501 |
| **可自动化** | ✅ Playwright |

### TC-A02: 普通用户不可见管理后台入口（P0）

| 项目 | 内容 |
|------|------|
| **ID** | TC-A02 |
| **前置条件** | 以普通用户（editor 角色，无 `system:manage_*` 权限）登录 |
| **步骤** | 1. 观察侧边栏底部 |
| **预期结果** | 不显示「管理后台」区域，不显示齿轮图标。底部只有历史记录折叠区 |
| **代码位置** | `app-sidebar.tsx` L413-416 `canAccessAdmin` |
| **可自动化** | ✅ Playwright |

### TC-A03: 有 system:manage_users 但非超管也能看到管理后台（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-A03 |
| **前置条件** | 用户有 `system:manage_users` 权限但 `isSuperAdmin = false` |
| **步骤** | 1. 观察侧边栏底部 |
| **预期结果** | 可见管理后台入口（`canAccessAdmin` 检查的是具体权限，不要求 `isSuperAdmin`） |
| **代码位置** | `app-sidebar.tsx` L413-416 |

### TC-A04: 空权限不等于管理权限（P3）

| 项目 | 内容 |
|------|------|
| **ID** | TC-A04 |
| **前置条件** | `permissions = []`（权限加载超时） |
| **步骤** | 1. 观察侧边栏底部 |
| **预期结果** | 不显示管理后台入口（`canAccessAdmin` 不使用 `permissions.length === 0` 容错） |
| **代码位置** | `app-sidebar.tsx` L413-416 |

---

## 模块四：首页配置隐藏

### TC-C01: 首页不显示能力配置区域（P0）

| 项目 | 内容 |
|------|------|
| **ID** | TC-C01 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 访问 `/home` 2. 在页面中搜索"能力配置"文字 3. 检查是否有折叠区域 |
| **预期结果** | 页面中不存在"能力配置"文字，不存在员工/知识库/工作流 Tab 切换 |
| **代码位置** | `home-client.tsx` 已移除 `renderConfigSection()` 调用 |
| **可自动化** | ✅ Playwright: `expect(page.locator('text=能力配置')).toHaveCount(0)` |

### TC-C02: 首页无空白区域残留（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-C02 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 访问 `/home` 2. 检查输入框下方是否有空白的 div 容器 |
| **预期结果** | 输入框下方无空白区域，布局紧凑 |
| **代码位置** | `home-client.tsx` 已移除空 `<div>` |

### TC-C03: renderConfigSection 函数定义保留（P2）

| 项目 | 内容 |
|------|------|
| **ID** | TC-C03 |
| **前置条件** | 开发环境 |
| **步骤** | 1. 在 `home-client.tsx` 中搜索 `renderConfigSection` |
| **预期结果** | 函数定义仍存在（未被删除），但没有被调用。不影响构建 |
| **代码位置** | `home-client.tsx` |

---

## 模块五：响应式布局

### TC-R01: 展开模式（≥1280px）侧边栏 200px（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-R01 |
| **前置条件** | 浏览器窗口 ≥1280px |
| **步骤** | 1. 观察侧边栏宽度 |
| **预期结果** | 侧边栏宽 200px，显示完整图标+文字，历史记录区域可见，管理后台可见（超管） |
| **代码位置** | `app-sidebar.tsx` L435 `w-[200px]` |

### TC-R02: 收起模式（768-1279px）侧边栏 60px（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-R02 |
| **前置条件** | 浏览器窗口 768-1279px |
| **步骤** | 1. 观察侧边栏宽度 2. 观察导航项显示 |
| **预期结果** | 侧边栏宽 60px，只显示图标。更多菜单通过 Popover 悬停弹出 |
| **代码位置** | `app-sidebar.tsx` L435 `w-[60px]` |

### TC-R03: 移动端（<768px）侧边栏隐藏（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-R03 |
| **前置条件** | 浏览器窗口 <768px（或 Chrome DevTools 设备模拟） |
| **步骤** | 1. 观察侧边栏是否隐藏 2. 点击底部浮动按钮 |
| **预期结果** | 侧边栏隐藏。底部有浮动按钮，点击后打开抽屉菜单，导航项完整 |
| **代码位置** | `dashboard-shell.tsx` 移动端布局 |

### TC-R04: 收起/展开切换过渡平滑（P2）

| 项目 | 内容 |
|------|------|
| **ID** | TC-R04 |
| **前置条件** | 桌面端 |
| **步骤** | 1. 点击侧边栏展开/收起按钮 2. 观察动画 |
| **预期结果** | 宽度在 200px↔60px 平滑过渡，图标和文字有渐变效果 |
| **可自动化** | ✅ Playwright + CSS transition 检查 |

---

## 模块六：回归验证

### TC-G01: 首页对话功能正常（P0）

| 项目 | 内容 |
|------|------|
| **ID** | TC-G01 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 在首页输入框输入"你好" 2. 按 Enter 发送 3. 等待 AI 回复 |
| **预期结果** | 消息正常发送，穆兰回复正常，对话历史正常展示。输入框、发送按钮、消息气泡无异常 |
| **可自动化** | ❌ AI 回复质量需要人工判断 |

### TC-G02: `/` 和 `@` 快捷输入正常（P1）

| 项目 | 内容 |
|------|------|
| **ID** | TC-G02 |
| **前置条件** | 以任意角色登录 |
| **步骤** | 1. 在输入框输入 `/` 2. 确认弹出技能菜单 3. 选择技能 4. 确认 chip 显示 5. 输入 `@` 6. 确认弹出员工菜单 7. 选择员工 8. 确认 chip 显示 |
| **预期结果** | `/` 和 `@` 菜单正常弹出，选择后 chip 正确显示，可取消，发送后参数正确传递 |
| **可自动化** | ❌ 弹出菜单交互需要人工判断 |

---

## 缺口分析

| 需求 | 对应用例 | 状态 |
|------|---------|------|
| 侧边栏一级导航精简 | TC-N01, TC-N02 | ✅ 已覆盖 |
| 更多菜单包含管理入口 | TC-M01, TC-M02, TC-M03 | ✅ 已覆盖 |
| 管理后台权限控制 | TC-A01, TC-A02, TC-A03, TC-A04 | ✅ 已覆盖 |
| 首页配置隐藏 | TC-C01, TC-C02, TC-C03 | ✅ 已覆盖 |
| 响应式三断点 | TC-R01, TC-R02, TC-R03 | ✅ 已覆盖 |
| 对话功能无回归 | TC-G01 | ✅ 已覆盖 |
| `/` `@` 快捷输入 | TC-G02 | ✅ 已覆盖 |

**覆盖率**: 100%（所有需求都有对应测试用例）

## 测试框架建议

| 类型 | 推荐工具 | 理由 |
|------|---------|------|
| E2E 自动化 | Playwright | 项目已有 Next.js，Playwright 对 React 支持好 |
| 组件测试 | Vitest + Testing Library | 项目使用 `useSearchParams` 等 Hook |
| 视觉回归 | Playwright 截图对比 | 响应式断点验证 |
