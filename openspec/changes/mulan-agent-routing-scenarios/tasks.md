## 1. OpenSpec 冲突收敛

- [x] 1.1 标记 `add-landing-page` 不再推进，避免与“引导页删除”冲突
- [x] 1.2 对照 `mulan-home-multi-turn-interaction`、`permission-system-fix`、`fix-chat-mission-sync-and-routing`，将未完成任务映射到本变更实现范围

## 2. 删除引导页并固定穆兰首页

- [x] 2.1 删除未挂载的 `src/app/landing/` 相关代码或确认无路由可访问
- [x] 2.2 确认根路径认证后进入 `/home`，未认证进入登录流程
- [x] 2.3 移除导航、文档和配置中仍指向引导页/落地页的入口

## 3. 自定义内容可见性

- [x] 3.1 修复自定义员工创建时缺少 `createdBy` 的问题
- [x] 3.2 员工列表页、首页和对话页传递管理员上下文，管理员可见组织内全部员工
- [x] 3.3 技能页普通用户仅展示自己创建的自定义技能，管理员展示组织内全部技能
- [x] 3.4 场景列表页普通用户仅展示自己创建的自定义场景，管理员展示组织内全部场景
- [x] 3.5 场景更新/删除 Server Action 增加创建者或管理员校验
- [x] 3.6 补充普通用户越权读取/修改/删除自定义内容的测试或手测记录

## 4. 工作流改名为场景

- [x] 4.1 将用户可见导航、页面标题、按钮、空状态和提示文案中的“工作流”改为“场景”
- [x] 4.2 保留内部 `workflow_templates` schema 和历史 API 命名，不做数据库迁移
- [x] 4.3 更新项目文档中面向产品的“工作流”表述

## 5. 穆兰意图识别契约

- [x] 5.1 调整 `IntentResult`/相关类型，穆兰路由目标只包含 LLM、员工、场景、Mission
- [x] 5.2 修改 `intent-recognition` prompt 和规则分支：员工优先，其次场景，不输出 skills 作为路由目标
- [x] 5.3 `/api/chat/intent` 加载可用员工和场景目录，并传给意图识别
- [x] 5.4 保留员工内部技能调用能力，但从穆兰推荐 UI 中移除技能直达入口

## 6. `/home` 穆兰界面内拉起员工和场景

- [x] 6.1 在 `/home` 穆兰对话中渲染单员工推荐卡片，确认后不跳转 `/chat`
- [x] 6.2 在 `/home` 穆兰对话中渲染多员工建群推荐，确认后内联创建群聊
- [x] 6.3 在 `/home` 穆兰对话中渲染场景推荐卡片，确认后内联启动场景或 Mission
- [ ] 6.4 复杂任务创建 Mission 后，在穆兰界面展示任务状态和参与员工
- [x] 6.5 移除穆兰推荐流程中 `window.location.href` 跳转到 `/chat` 的行为

## 7. `/chat` 保留但穆兰置顶

- [ ] 7.1 确认员工列表始终将穆兰置顶，包括存在自定义员工时
- [x] 7.2 保留 `/chat` 手动建群能力，作为对话中心功能而非穆兰推荐必需路径

## 8. BullMQ 收口

- [x] 8.1 更新 OpenSpec 配置和项目文档中的 Inngest 残留描述
- [x] 8.2 审计 BullMQ queue key 与实际 queue name 的一致性
- [x] 8.3 注册 `intentDispatch` worker，或删除未使用的死队列定义
- [x] 8.4 确认新增异步意图/任务分发不引入 Inngest 依赖

## 9. 验证

- [x] 9.1 运行 `npx tsc --noEmit`
- [x] 9.2 运行 `npm run build`
- [ ] 9.3 手动验证：普通用户只能看到自己创建的员工、技能、场景
- [ ] 9.4 手动验证：管理员能看到组织内所有员工、技能、场景
- [ ] 9.5 手动验证：穆兰简单对话、单员工推荐、场景推荐、多员工建群、复杂 Mission 创建五条路径
